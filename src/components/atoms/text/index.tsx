import { Typography } from '@mui/material';
const Text = () => {
  return (
    <div>
      <Typography variant="h1" sx={{ color: 'teal' }}>
        Hello World
      </Typography>

      <Typography variant="h2" sx={{ color: 'violet' }}>
        Hello World
      </Typography>

      <Typography variant="h5" sx={{ color: 'red' }}>
        Hello World
      </Typography>
    </div>
  );
};

export default Text;
